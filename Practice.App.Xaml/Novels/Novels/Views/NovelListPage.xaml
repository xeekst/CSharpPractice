<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="Novels.Views.NovelListPage">
    <ContentPage.Content>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="100" />
                <RowDefinition Height="60" />
                <RowDefinition Height="50" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <Label Text="Temukan Artikel Favorit Kamu Disini!"
                   HorizontalOptions="Center"
                   VerticalOptions="Center">
            </Label>
            <SearchBar Grid.Row="1"
                       Placeholder="Search"
                       CancelButtonColor="Orange"
                       FontSize="Medium"
                       VerticalOptions="Center"
                       HorizontalOptions="Center"
                       FontAttributes="Italic"
                       BackgroundColor="#fff">
            </SearchBar>
            <FlexLayout Grid.Row="2"
                        Direction="Row"
                        AlignItems="Center"
                        AlignContent="Center"
                        JustifyContent="Center"
                        VerticalOptions="Center"
                        HorizontalOptions="Center">
                <Label Text="中："></Label>
                <Switch VerticalOptions="Center" HorizontalOptions="Center"></Switch>
                <Label Text="英：" Margin="30,0,0,0"></Label>
                <Switch VerticalOptions="Center" HorizontalOptions="Center"></Switch>

            </FlexLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>
